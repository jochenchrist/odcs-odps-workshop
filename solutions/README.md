# Solutions

Reference solutions for verification purposes only, one folder per exercise. Try the exercises yourself first!

Run `./solutions/test_all.sh` from the repository root to recheck all solutions sequentially, exercise by exercise. Set `ENTROPY_DATA_API_KEY` and `ENTROPY_DATA_HOST` to also run the publish step of exercise 7.
